# abhishek4official.github.io

This repository hosts the user site for the GitHub Pages account `abhishek4official` at `https://abhishek4official.github.io`.

This commit adds a minimal `index.html` and a `.nojekyll` file so the repository immediately serves a basic static page. Replace the contents with your own HTML/CSS or add a static-site generator (Jekyll, Hugo, Next.js, etc.) if you prefer.

To publish changes, commit to the `main` branch (or the repository's default branch). GitHub Pages will serve the site automatically for the `username.github.io` repository.
